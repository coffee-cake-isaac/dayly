name: dayly
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  calendar_timeline: ^1.1.3
  flutter:
    sdk: flutter
  get:
  http: ^1.2.2
  intl: ^0.19.0
  stacked_listview: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
